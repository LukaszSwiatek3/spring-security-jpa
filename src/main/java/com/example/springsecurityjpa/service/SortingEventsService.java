package com.example.springsecurityjpa.service;

public class SortingEventsService {

//    public Events sortByRating(Events events){
//        Comparator<Event> compareByRating = Comparator.comparing(event1 -> event1.getRating());
//        Collections.sort(events.getEvents(), compareByRating.reversed());
//        return events;
//    }

//    public Events sortByPopularity(Events events){
//        Comparator<Event> compareByPopularity = Comparator.comparing(event1 -> event1.getPopularity());
//        Collections.sort(events.getEvents(), compareByPopularity.reversed());
//        return events;
//    }

//    public Events sortByDistance(Events events){
//        Comparator<Event> compareByDistance = Comparator.comparing(event1 -> event1.getDistance());
//        Collections.sort(events.getEvents(), compareByDistance.reversed());
//        return events;
//    }

}
